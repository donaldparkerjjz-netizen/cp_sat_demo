# 工厂排工排产系统 · 智能排产 Demo（CP-SAT）

> 依据《PRD_工厂排工排产系统.md》Phase 1 范围落地：把「基础数据 → 智能排产引擎 → 交互式甘特图 → 排产分析 → 插单重排」串成一个可运行的 Web 演示，用于验证排产模型与可视化。

## 特性

- **基础数据管理**：机台/返工专线（归班组）、产品与标准产能、计件工价、产品-机台适配矩阵、换模矩阵、订单（含交期/优先级/类型/来源）、班次日历。
- **智能排产引擎（OR-Tools CP-SAT）**：
  - 硬约束：产品-机台适配、单班唯一（一机台一班组一产品）、产能上限（按班产能折算所需班次）、返工专线、换模/清机预留（不同产品相邻班次留空档）、订单连续性。
  - 软目标（可按预设切换）：优先级加权拖期、交期准入、工价均衡（各机台员工累计计件 ±20% 带）、换模最小化、利用率。
- **交互式甘特图**：按资源/按班组两种视图；产品着色；换模 ▲ 标注；交期红虚线；完成圆点；悬停详情；滚轮缩放、拖拽平移。
- **排产分析仪表盘**：机台利用率与瓶颈、产能 vs 需求（超载预警，已按休息日扣除可用班次）、工价均衡（±20% 带）、拖期/准交率、换模次数、工厂日历与休息日、在制/峰值、排产类型（正常/返工/委外）统计、关键 KPI 卡片。
- **工厂日历**：可配置休息日（默认第 7 天周日休息），休息日硬禁止排产，甘特图灰显，产能/利用率按可用班次计算。
- **甘特图导出**：一键导出排产甘特图 PNG、排产矩阵 CSV(Excel)。
- **通用数据接入（自动逐页解析，多格式）**：面向通用排工排产系统，无需按模板整理——可上传 .xlsx（多工作表）/ .csv / 多文件，系统<b>逐页自动识别</b>（表头探测 + 页名/列头关键词匹配）：产品/工价表、机台台账、产品-机台适配、订单、换模矩阵/清机、日历/休息日、<b>日产量矩阵（机台或产品 × 日期）</b>等；日产量矩阵自动折算为订单/需求；多文件同类数据自动合并。说明/排产结果等输出页自动跳过；每文件/每页给出处理清单；未识别页列明原因不盲塞。
  - 解析出的产品/资源/适配/订单/日历会<b>替换</b>对应默认数据集；文件未提供的维度保持默认。
  - 支持把排产结果（机台 x 班次矩阵 + 订单排产）回写 Excel（对应 PRD 7.2 数据接入 / 7.4 导出 / 7.6 结果回写）。
  - 若遇极不规则格式，可在 demo_ingest.py 的字段别名/页名提示中按需扩展，实现配置化适配。
  - **多工作表**：逐页识别——命中模板结构的产品/机台/适配/订单/换模/日历页会导入；其余页面列为「未匹配」，并给出原文件完整工作表清单与各页处理结果，便于核对。益丰生产表单（织造/后整流水线）的页面会被列出但不会强映射进车间排产。
- **插单重排**：插入一张急单，系统在硬约束下自动寻优机台与班次；给出插单前后 KPI 对比、被挤/受影响订单列表，以及 P1（同产品其他可用机台）/ P2（原机台顺延）/ P3（跨机台跨日期，需人工确认）调剂建议。
- **自然语言约束（AI 助手雏形）**：企业方操作员用日常中文描述硬/软约束（交期、固定机台、产品适配、机台停用、班次上限、换模最少、工价均衡、优先排产等），系统解析为结构化约束并应用到 CP-SAT 模型，支持“解析 + 应用并重排”。

## 目录

    cp_sat_demo/
    ├── demo_engine.py        # 核心引擎：基础数据、CP-SAT 建模与求解、排产分析、插单重排、NL 约束应用
    ├── demo_constraint_nlu.py# 自然语言约束解析（实体识别 + 意图匹配 -> 结构化硬/软约束）
    ├── demo_excel_io.py      # Excel 模板生成 / 结果回写
    ├── demo_ingest.py        # 通用数据接入：逐页自动识别多格式( xlsx 多页/csv/多文件/日产量矩阵 )并组装场景
    ├── demo_server.py        # FastAPI 后端（/，/api/data，/api/solve，/api/insert，/api/nlu，/api/apply，/api/ingest，/api/excel/*，/api/reset）
    ├── demo_static/demo.html # 前端单页（内联 CSS/JS，甘特图 + 分析 + 插单 + 基础数据 + 智能约束）
    ├── demo_schedule.json    # 默认场景求解示例输出
    └── libs/                 # 已解压的 ortools / openpyxl（离线可运行）

## 快速开始

环境：Python 3.12（已内置 OR-Tools CP-SAT 与 FastAPI，libs/ 中离线依赖）。

    # 方式一：在线演示（推荐）
    python demo_server.py
    浏览器打开  http://127.0.0.1:8080

在页面上：
1. 默认自动求解，展示「排产甘特图」；可切换「按资源 / 按班组」视图。
2. 顶部下拉切换「优化目标」后点「重新求解」，观察不同侧重下的方案差异。
3. 切到「排产分析」查看利用率、瓶颈、产能与需求、工价均衡（±20% 带）、拖期/准交。
4. 切到「插单重排」，输入一张急单（产品/数量/交期/优先级/类型），点「插入并重排」，查看前后对比与 P1/P2/P3 调剂建议。
5. 切到「基础数据」查看机台、产品工价、适配矩阵与订单。
6. 切到「智能约束」，输入自然语言（如“O01必须在第3班前完成；P3只能在M5和M6上生产；换模次数越少越好”），点「解析约束」查看识别结果，点「应用并重排」应用到模型并重排。

### 命令行求解

    python demo_engine.py     # 求解默认减振车间场景，输出 demo_schedule.json 并打印摘要

## HTTP API

| 方法 | 路径         | 说明 |
|------|--------------|------|
| GET  | /            | 返回前端单页 |
| GET  | /api/data    | 当前场景基础数据 |
| GET  | /api/solve?objective=balanced&max_time_s=30 | 求解并返回排程 + 分析 JSON |
| POST | /api/insert  | body: {order:{...},max_time_s:30}，插入急单并重排 |
| POST | /api/nlu     | body: {text:"..."}，解析自然语言约束（不应用） |
| POST | /api/apply   | body: {text:"...",objective,max_time_s}，解析并应用约束 + 重排 |
| GET  | /api/excel/template | 下载排产数据 Excel 模板 |
| POST | /api/excel/import | body: {data_base64,objective,max_time_s}，解析上传 Excel 并覆盖场景重排 |
| POST | /api/excel/export | body: {objective,max_time_s}，回写排产结果 Excel 下载 |
| POST | /api/ingest   | body: {files:[{name,data_base64}],objective,max_time_s}，通用多文件逐页解析并重排 |
| POST | /api/reset   | 恢复默认场景 |

`/api/solve` 的 objective 取值：balanced（多目标均衡）/ priority（优先级最大化）/ wage（工价均衡优先）/ changeover（换模最小化）。

    curl "http://127.0.0.1:8080/api/solve?objective=wage&max_time_s=30"... (line truncated to 2000 chars)