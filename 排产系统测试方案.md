# 益丰整经织造排产系统测试方案

## 1. 测试目标

验证系统能够把订单要求转换为一周整经与织造计划，并在算法和前端中一致落实以下规则：

1. 订单按交期升序、优先级降序进入计划。
2. 织造初排必须考虑织机适配、产能、停机状态和换款成本。
3. 生产顺序严格为“整经 → 穿综穿筘 → 上轴准备 → 织造”。
4. 每段可执行织造必须绑定具体经轴，且累计使用米数不得超过该轴可用米数。
5. 非机上经轴必须在织造准备前至少 120 分钟到位。
6. 经轴不足时只能补排整经或缩减织造量，不得自动生成无来源经轴。
7. 所有事件必须落在 7 天计划窗口内。
8. 已上机且余量充足的经轴允许跨日连续织造；后续日期不要求重复整经。
9. 前端必须区分“CP-SAT 织造初排”和“逐轴校验后的最终可执行计划”。

## 2. 测试范围

- 后端算法：订单排序、整经计划、经轴台账、逐轴分配、准备工序、七天边界、数量缩减和计划追溯。
- API：健康检查、最近排程、联动排程结果及关键统计字段。
- 前端：首页联动摘要、整经依据、最终可执行织造表、算法流程、逐轴决策与一周工艺时间轴。
- 工程质量：全部自动化测试与生产构建。

暂不测试真实设备通讯、MES/ERP 写回、真实经轴扫码、真实整经机并行能力和正式生产下达。

## 3. 测试环境

| 项目 | 配置 |
|---|---|
| 操作系统 | Windows |
| 后端 | Python + FastAPI + OR-Tools CP-SAT |
| 前端 | React + TypeScript + Vite |
| API 地址 | `http://127.0.0.1:8001` |
| 前端地址 | `http://127.0.0.1:5173` |
| 排程周期 | 2026-04-01 00:00 至 2026-04-08 00:00，左闭右开 |
| 当前数据 | 客户工作簿导入数据 + 明确标记的模拟参数 |

## 4. 测试数据策略

- 使用当前客户工作簿生成的场景进行端到端验证。
- 使用最小可控场景精确验证业务边界：单订单、单织机、单经轴、跨日续织、缺轴、补排和窗口不足。
- 所有模拟轴均必须具有来源任务编号，不允许以无来源的 `SIM-*` 或其他临时编号绕过校验。

## 5. 测试用例

| 编号 | 测试内容 | 操作/输入 | 预期结果 |
|---|---|---|---|
| ALG-01 | 订单排序 | 不同交期和优先级的 3 个订单 | 先按交期，再按优先级排整经 |
| ALG-02 | 工艺顺序 | 需要改品番仕挂的订单 | 整经完成后才能穿综、准备和织造，各事件无逆序 |
| ALG-03 | 具体经轴绑定 | 单订单、单轴 | 织造事件包含具体轴号和来源整经任务 |
| ALG-04 | 提前两小时到位 | 非机上经轴 | 经轴到位时间不晚于准备开始前 120 分钟 |
| ALG-05 | 跨日续织 | 3,600 米经轴承载 2,000 米织造 | 织造从前期持续到 4 月 6 日，仅使用原轴，4 月 6 日无需新增整经 |
| ALG-06 | 经轴不足 | 品番无可追溯经轴 | 可执行量为 0 或明确缩减，不生成无来源经轴 |
| ALG-07 | 显式补排 | 有品番主档但台账无轴 | 生成带来源任务的补排整经，再允许织造 |
| ALG-08 | 七天边界 | 需求超过剩余产能 | 超出部分缩减，所有事件结束时间不超过第 7 天末 |
| ALG-09 | 四种仕挂 | 构造线边余轴、接经、原品番和改品番状态 | 四种状态均正确分类 |
| ALG-10 | 故障避让 | 织机存在停机窗口 | 织造和准备事件不与停机区间重叠 |
| API-01 | 服务健康 | 请求 `/api/health` | HTTP 200，状态为 `ok` |
| API-02 | 联动结果 | 请求 `/api/schedules/latest` | 返回 `execution_preview`、六阶段流程和逐订单决策 |
| API-03 | 数量守恒 | 核对联动 KPI | 初排量 = 可执行量 + 缩减量 |
| UI-01 | 首页摘要 | 打开排程看板 | 显示可执行量、缩减量、绑定轴段和逐轴决策入口 |
| UI-02 | 整经依据 | 打开“经轴与整经” | 显示订单交期、优先级、轴号及排产依据 |
| UI-03 | 最终计划 | 检查最终可执行织造表 | 显示到位时间、最晚到位、实际织造、缩减原因 |
| UI-04 | 初排隔离 | 检查原 CP-SAT 表 | 明确标记为“尚未逐轴校验，不可直接下达” |
| UI-05 | 流程与时间轴 | 打开“工况模拟” | 显示六阶段规则和时间竖轴/工艺横轴的一周计划 |
| ENG-01 | 后端回归 | 执行全部 Pytest | 全部通过 |
| ENG-02 | 前端回归 | 执行 Vitest | 全部通过 |
| ENG-03 | 生产构建 | 执行 TypeScript + Vite build | 构建成功，无阻断错误 |

## 6. 通过准则

- ALG-01 至 ALG-10、API-01 至 API-03、UI-01 至 UI-05 全部通过。
- 后端和前端自动化测试无失败。
- 生产构建成功。
- 不存在未绑定经轴却被计入可执行织造量的记录。
- 不存在超出七天窗口的生产事件。

## 7. 风险与限制

- 当前订单交期、统一织造效率、部分经轴编号和准备工时来自推导或模拟值。
- 源数据缺少部分产品—经轴品番映射，这些订单应被缩减而不是猜测映射。
- 当前整经按单一计划池串行模拟；取得真实整经机台、班次和速度后需重新执行容量测试。
