# 益丰整经织造排产系统下一阶段实施建议

## 1. 当前状态

系统已经完成一周排产原型，具备以下能力：

- 生成 7 天整经计划和织造初排；
- 按订单交期和优先级安排整经顺序；
- 生成逐根经轴台账；
- 按“整经 → 穿综穿筘 → 上轴准备 → 织造”模拟执行；
- 每段织造绑定具体经轴；
- 校验经轴数量、剩余米数、提前 120 分钟到位和七天边界；
- 缺轴时补排整经或缩减织造，不允许凭空生成经轴；
- 在前端区分 CP-SAT 织造初排和逐轴校验后的最终可执行计划。

当前排程中，CP-SAT 初排为 21,218 米，逐轴联动校验后可执行量为 9,361.109 米，缩减量为 11,856.891 米。这说明当前“织造初排”和“上游准备能力”之间仍存在较大差距。

## 2. 下一阶段核心目标

下一阶段的核心不是继续增加展示页面，而是把整经、经轴、穿综、上轴和织造约束直接纳入主求解模型。

目标是让算法首次输出的结果就具备可执行性，减少“先排出织造计划，再在模拟阶段大量缩减”的情况。

最终应形成统一排程链路：

```text
订单需求
  ↓
产品、工艺和织机适配检查
  ↓
联合选择织机、经轴和整经时段
  ↓
安排穿综穿筘与上轴准备
  ↓
生成七天可执行织造计划
  ↓
现场反馈与滚动重排
```

## 3. 第一优先级：整经—织造联合优化

### 3.1 模型需要同时决定的内容

主算法应同时决定：

- 哪些订单进入本周计划；
- 每张订单分配到哪台织机；
- 是否允许拆分以及拆分为多少个织造段；
- 每个织造段使用哪根具体经轴；
- 每根经轴在什么时间整经；
- 何时进行穿综穿筘、接经或改品番仕挂；
- 何时完成上轴准备；
- 织造实际开始和结束时间；
- 哪些需求因产能、物料或经轴不足未排入本周。

### 3.2 必须进入主模型的硬约束

1. 工艺顺序约束：

   ```text
   整经完成时间
   ≤ 穿综穿筘开始时间
   ≤ 上轴准备开始时间
   ≤ 织造开始时间
   ```

2. 经轴提前到位约束：

   ```text
   经轴到位时间 ≤ 织造准备开始时间 - 120 分钟
   ```

3. 经轴容量约束：

   ```text
   同一根经轴累计织造米数 ≤ 经轴可用米数
   ```

4. 资源不重叠约束：

   - 同一织机同一时间只能执行一个任务；
   - 同一整经资源同一时间只能整经一根轴；
   - 同一穿综资源同一时间只能处理一个任务；
   - 停机、维修和不可用班次内不得安排生产。

5. 工艺适配约束：

   - 产品只能分配给兼容织机；
   - 产品必须使用已经确认的经轴品番；
   - 钢筘、边撑和其他工装必须满足工艺要求；
   - 映射缺失不得通过名称猜测或默认全兼容处理。

6. 七天边界约束：

   - 整经、穿综、准备和织造事件全部位于本周窗口内；
   - 跨周需求只计算本周实际可完成量；
   - 超出部分进入未排任务，不允许隐藏在窗口外。

7. 经轴不足处理：

   - 有完整经轴主档且本周整经能力允许时，可以补排整经；
   - 缺少产品—经轴映射时，不允许补排未知经轴；
   - 无法补排时缩减织造数量，并记录明确原因。

### 3.3 优化目标建议

采用分层优化，按照以下优先顺序求解：

1. 最大化订单需求覆盖量；
2. 最小化订单延期；
3. 最大化高优先级订单完成量；
4. 最小化换款、穿综、接经和上轴次数；
5. 最小化织机空闲和计划波动；
6. 尽量保持同品番连续整经和连续织造。

低优先级目标不得以违反经轴、物料、工艺顺序或七天边界为代价。

## 4. 第二优先级：模拟数据维护页面

在真实客户数据尚未完整取得前，需要增加可编辑的模拟数据页面。

### 4.1 订单数据

- 订单编号；
- 产品编号；
- 需求数量；
- 交期；
- 优先级；
- 是否允许拆单；
- 最小拆分量；
- 锁定状态。

### 4.2 产品与工艺数据

- 产品对应经轴品番；
- 产品对应织造品番和水洗品番；
- 可生产织机清单；
- 所需钢筘、边撑及其他工装；
- 标准经轴长度；
- 产品在不同织机上的实际生产效率。

### 4.3 经轴数据

- 实体经轴编号；
- 经轴品番；
- 初始米数；
- 当前剩余米数；
- 当前状态；
- 当前所在位置；
- 当前绑定织机；
- 计划完成时间；
- 来源整经任务。

### 4.4 设备与资源数据

- 织机当前产品和当前经轴；
- 织机可用班次；
- 停机和维修时间；
- 整经机数量、班次和速度；
- 穿综资源数量和班次；
- 换款、接经、上轴和落布标准工时。

所有模拟值必须带有“模拟数据”标识，真实数据接入后可以直接替换，而不改变算法结构。

## 5. 第三优先级：七天滚动重排

系统应每天或每班根据现场数据重新计算未来七天。

### 5.1 滚动流程

```text
读取上一版计划
  ↓
采集实际完成量、剩余轴长、设备状态和物料状态
  ↓
冻结已经开始或近期不能调整的任务
  ↓
更新订单剩余需求
  ↓
重新求解未来七天
  ↓
输出新计划与上一版计划的差异
```

### 5.2 计划冻结建议

- 已经开始执行的任务完全冻结；
- 未来 2 小时内即将执行的任务原则上冻结；
- 人工锁定的订单、织机和经轴不得自动调整；
- 冻结区外任务允许重新分配，但应尽量减少计划变化。

### 5.3 差异输出

每次重排应展示：

- 新增任务；
- 取消任务；
- 推迟或提前的任务；
- 更换织机的任务；
- 更换经轴的任务；
- 新增整经或补轴任务；
- 因异常缩减的生产数量；
- 受影响订单及预计交期变化。

## 6. 第四优先级：异常工况模拟

建议增加以下异常场景：

| 异常场景 | 期望处理 |
|---|---|
| 经轴提前用完 | 查找下一根合格经轴；不足则补排整经或停机 |
| 整经机故障 | 推迟受影响经轴，重新计算织造开工时间 |
| 织机临时停机 | 冻结已完成量，剩余任务换机或延期 |
| 紧急订单插单 | 在冻结区外重新优化，显示被影响订单 |
| 原料延期 | 禁止相关整经任务开工，并更新交期预测 |
| 实际效率下降 | 根据实际产量重新估算完成时间和剩余需求 |
| 经轴质量异常 | 禁止该轴继续使用，转入退轴或返工状态 |
| 订单数量变化 | 保留已执行部分，对剩余需求重新排程 |

每种异常都必须记录触发原因、调整前计划、调整后计划和受影响订单。

## 7. 需要向客户索取的数据

### 7.1 第一批：联合排程最低必要数据

1. 真实订单编号、产品、数量、交期和优先级；
2. 产品与经轴品番的完整对应关系；
3. 当前每台织机生产的产品、使用经轴和剩余米数；
4. 实体经轴编号、品番、总米数、剩余米数和所在位置；
5. 产品在不同织机上的实际日产量；
6. 整经机数量、班次和单轴整经时间；
7. 穿综、上轴、接经、换款和落布的标准工时。

### 7.2 第二批：提高计划准确度的数据

- 纱线实际库存与确认到货时间；
- 钢筘、边撑和工装库存；
- 设备维修计划和历史故障率；
- 产品质量限制和不可连续生产组合；
- 订单拆分规则和最小生产批量；
- 客户允许提前或延期的范围；
- 历史实际产量和工时记录。

## 8. 建议实施步骤

### 步骤一：重构主算法

- 将经轴实例、整经任务和准备任务纳入主求解模型；
- 在主模型中建立订单—产品—织机—经轴的追溯关系；
- 删除“初排成功但模拟阶段无法执行”的隐式口径；
- 保留初排数据作为求解诊断，不作为生产计划。

### 步骤二：建设模拟数据维护功能

- 提供订单、经轴、织机和工艺参数的新增、编辑及导入；
- 支持恢复默认模拟数据；
- 显示真实数据和模拟数据来源；
- 对缺失字段进行提交前校验。

### 步骤三：实现滚动重排

- 支持读取上一版计划；
- 支持冻结已执行和近期任务；
- 支持保存每次排程版本；
- 增加计划差异页面。

### 步骤四：补充异常测试和客户验收

- 自动执行故障、插单、缺料和缺轴场景；
- 使用客户提供的数据重新运行测试方案；
- 由现场计划员逐单核对整经、经轴和织造计划；
- 通过后再开放正式计划导出或系统写回。

## 9. 下一阶段验收标准

### 9.1 算法验收

- 100% 的可执行织造段绑定具体经轴；
- 100% 的经轴有明确来源、米数和可用时间；
- 不存在织造早于整经、穿综或准备完成的情况；
- 不存在超出经轴剩余米数的织造计划；
- 不存在超过七天边界的事件；
- 初排量与最终可执行量的差异应显著下降；
- 所有未排数量均有明确、可追溯的原因。

### 9.2 前端验收

- 计划员可以从订单追溯到织机、经轴和整经任务；
- 可以查看经轴每天的剩余米数；
- 可以识别跨日续织和换轴节点；
- 可以区分正式数据、推导数据和模拟数据；
- 可以查看计划版本差异和异常调整原因；
- 页面不再把未经逐轴校验的初排误认为正式计划。

### 9.3 测试验收

- 后端、前端自动化测试全部通过；
- 缺轴、故障、插单、缺料和效率变化测试全部通过；
- 现场抽取不少于 10 张真实订单进行逐单核对；
- 计划员确认排程结果具备现场可执行性。

## 10. 推荐结论

下一步建议立即实施“整经—织造联合优化”。这是当前系统从原型演示走向可执行排产的关键环节。

模拟数据维护、滚动重排和异常工况应在联合模型稳定后依次实施。真实数据可以后续导入，但算法的数据结构和约束应先按照真实生产场景设计，避免在客户数据到位后再次大规模重构。
