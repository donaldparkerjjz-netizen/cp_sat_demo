# 益丰整经织造系统非排产模块对齐优化方案

## 1. 背景

当前一周整经—织造排产已经能够运行，但真实生产规则仍需等待领导和客户确认。因此，现阶段不宜继续扩大核心排产算法的业务假设。

在等待反馈期间，可以先对不依赖最终排产规则的模块进行对齐和优化，提升系统的数据质量、可解释性、稳定性和后续维护效率。

本阶段的原则是：

- 不修改尚未确认的排产业务规则；
- 不把模拟结果包装成正式生产计划；
- 优先完善数据、参数、状态、错误处理和计划追溯；
- 所有优化均为后续联合排程和真实数据接入打基础。

## 2. 本阶段目标

1. 建立统一的数据质量检查入口；
2. 集中管理模拟参数和业务假设；
3. 对齐订单、产品、织机、经轴等基础数据口径；
4. 优化前后端异常提示，解决 `Failed to fetch` 难以定位的问题；
5. 保存排程版本、输入快照和参数，支持结果追溯；
6. 统一页面名称、状态、日期、单位和数据来源标识；
7. 完善 Excel 导入检查、结果导出和自动化测试。

## 3. 本阶段范围

### 3.1 可以立即优化

- 数据质量中心；
- 模拟参数管理；
- 任务池数据展示；
- 织机资源台账；
- 经轴数据台账；
- 数据来源与更新时间；
- 前后端服务状态；
- 错误提示与重试；
- 排程版本记录；
- 计划差异展示；
- Excel 导入预检查；
- 排程结果导出；
- 页面术语、状态和样式统一；
- 自动化测试与运行日志。

### 3.2 暂缓修改

以下内容需要等待领导或客户确认：

- 订单优先级的最终计算方式；
- 是否允许拆单以及最小拆分量；
- 整经与织造的最终联合优化方式；
- 经轴提前到位时间；
- 穿综、接经、上轴、换款和落布工时；
- 产品和织机真实产能；
- 整经机真实数量、班次和并行规则；
- 工装冲突和工艺禁配规则；
- 正式生产计划发布与系统写回。

## 4. 模块一：数据质量中心

### 4.1 建设目的

将当前分散在各页面的数据警告集中展示，让计划员知道哪些数据缺失、哪些是模拟值，以及这些问题会影响哪些订单。

### 4.2 页面内容

数据质量中心建议包含以下统计：

- 完整订单数；
- 缺交期订单数；
- 缺优先级订单数；
- 缺经轴映射产品数；
- 缺织机适配产品数；
- 缺生产效率记录数；
- 缺实体轴号数量；
- 缺经轴剩余米数数量；
- 缺物料到货时间数量；
- 使用模拟参数数量。

### 4.3 异常明细字段

| 字段 | 说明 |
|---|---|
| 数据对象 | 订单、产品、织机、经轴、物料或工装 |
| 对象编号 | 对应业务编号 |
| 问题类型 | 缺失、格式错误、重复、过期或相互矛盾 |
| 严重程度 | 阻断、警告、提示 |
| 影响范围 | 受影响订单、工序或设备 |
| 当前处理 | 阻止排产、使用模拟值或仅提示 |
| 数据来源 | Excel 表名、模拟配置或人工录入 |
| 更新时间 | 最近数据更新时间 |
| 建议动作 | 需要向客户确认或补充的内容 |

### 4.4 功能要求

- 按严重程度、数据对象和问题类型筛选；
- 点击异常跳转至相关订单、织机或经轴；
- 导出“待客户补充数据清单”；
- 已确认数据可标记为已处理；
- 重新导入数据后自动重新检查；
- 阻断问题不得被普通用户静默忽略。

## 5. 模块二：模拟参数管理

### 5.1 建设目的

集中管理当前用于原型验证的所有模拟参数，避免参数散落在代码中，导致使用人员无法判断结果依据。

### 5.2 参数分类

#### 产能参数

- 默认织造效率；
- 产品—织机专属效率；
- 整经单轴工时；
- 整经计划池每日可用时长；
- 穿综资源每日可用时长。

#### 准备工时参数

- 经轴提前到位时间；
- 直接续产准备时间；
- 接经准备时间；
- 原品番仕挂时间；
- 改品番仕挂时间；
- 落布时间。

#### 计划控制参数

- 默认排程周期；
- 冻结天数；
- 是否允许拆单；
- 最小拆分量；
- 求解时限；
- 适配模式；
- 是否启用物料和经轴约束。

### 5.3 参数字段

每项参数至少记录：

- 参数名称；
- 参数编码；
- 当前值和单位；
- 数据来源；
- 真实值或模拟值；
- 是否已经客户确认；
- 生效时间；
- 修改人员；
- 修改原因；
- 上一版本值。

### 5.4 页面功能

- 查看和编辑参数；
- 未确认参数醒目标识；
- 恢复默认模拟值；
- 保存参数版本；
- 查看修改历史；
- 排程结果显示实际使用的参数版本；
- 修改参数后提示重新运行排程。

## 6. 模块三：基础数据与资源台账对齐

### 6.1 订单任务池

统一展示：

- 订单编号；
- 产品编号；
- 需求数量；
- 已完成数量；
- 剩余数量；
- 交期；
- 优先级；
- 是否允许拆单；
- 当前排产状态；
- 未排原因；
- 数据来源和更新时间。

订单状态建议统一为：

```text
待排产 / 部分排入 / 已排入 / 执行中 / 已完成 / 已暂停 / 数据异常
```

### 6.2 产品工艺主档

统一维护：

- 产品编号；
- 产品背番号；
- 经轴品番；
- 织造品番；
- 水洗品番；
- 允许使用的织机；
- 钢筘和工装要求；
- 经轴标准长度；
- 生产效率；
- 映射完整性；
- 数据来源。

映射缺失时必须显示“待建档”，不得通过产品名称自动推断。

### 6.3 织机资源台账

统一展示：

- 织机编号；
- 设备状态；
- 当前产品；
- 当前经轴编号；
- 当前经轴剩余米数；
- 当前任务预计结束时间；
- 可生产产品；
- 班次和可用时间；
- 维修和停机计划；
- 数据更新时间。

织机状态建议统一为：

```text
运行 / 待机 / 准备 / 停机 / 维修 / 故障 / 数据待确认
```

### 6.4 经轴台账

统一展示：

- 实体或推导轴号；
- 经轴品番；
- 总米数；
- 已使用米数；
- 剩余米数；
- 当前状态；
- 所在位置；
- 绑定织机；
- 来源整经任务；
- 整经完成时间；
- 数据来源和更新时间。

经轴状态建议统一为：

```text
待整经 / 整经中 / 待穿综 / 待上轴 / 已上机 / 使用中 / 已用完 / 质量异常
```

## 7. 模块四：前后端连接与错误处理

### 7.1 当前问题

前端出现 `Failed to fetch` 时，使用人员无法判断是后端未启动、地址错误、请求超时，还是接口内部异常。

### 7.2 优化方案

前端应将错误分类为：

| 错误类型 | 页面提示 |
|---|---|
| 后端无法连接 | 排程服务未启动或网络不可达，请检查服务状态 |
| 请求超时 | 排程计算时间过长，请稍后重试或增加求解时限 |
| HTTP 4xx | 请求参数或数据不符合要求，并显示具体字段 |
| HTTP 5xx | 后端处理失败，显示错误编号并保留已有结果 |
| 返回格式异常 | 服务返回数据不完整，请联系维护人员 |
| 局部接口失败 | 仅对应模块显示错误，不清空整个排程页面 |

### 7.3 服务状态展示

建议在页面右上角增加：

- 前端状态；
- 后端连接状态；
- 最近成功连接时间；
- 当前 API 地址；
- 当前算法版本；
- 当前数据快照版本；
- 手动重新连接按钮。

### 7.4 稳定性要求

- 请求失败时保留最后一次成功数据；
- 普通查询支持安全重试；
- 排程提交使用请求编号，避免重复点击生成多份计划；
- 后端错误包含可追踪的错误编号；
- 页面错误信息使用中文业务描述，不直接显示底层异常堆栈。

## 8. 模块五：排程版本与审计

### 8.1 版本记录

每次运行排程应保存：

- 排程编号；
- 创建时间；
- 创建人员；
- 输入数据快照；
- 参数版本；
- 算法版本；
- 排程周期；
- 求解状态；
- 业务状态；
- 初排数量；
- 可执行数量；
- 缩减数量；
- 数据质量状态。

### 8.2 版本状态

建议使用：

```text
草稿 / 模拟完成 / 待审核 / 已确认 / 已发布 / 已作废
```

在领导确认业务流程之前，系统生成的版本最高只能进入“模拟完成”，不得进入“已发布”。

### 8.3 版本差异

两个排程版本之间应展示：

- 新增或取消的订单；
- 数量变化；
- 开始和结束时间变化；
- 织机变化；
- 经轴变化；
- 整经任务变化；
- 未排原因变化；
- 数据和参数版本变化。

### 8.4 审计记录

所有人工调整记录：

- 调整对象；
- 调整前值；
- 调整后值；
- 调整人员；
- 调整时间；
- 调整原因；
- 是否重新运行排程。

## 9. 模块六：前端展示口径统一

### 9.1 计划层级

系统统一使用以下名称：

| 名称 | 含义 | 是否可下达 |
|---|---|---|
| 织造初排 | CP-SAT 对订单和织机的数学分配 | 否 |
| 模拟可执行计划 | 通过经轴和工艺顺序校验的计划 | 否，需人工确认 |
| 正式下达计划 | 领导或计划员审核发布的生产指令 | 是 |

### 9.2 数据来源标签

- 真实数据：客户或现场系统提供；
- 推导数据：根据客户表格计算获得；
- 模拟数据：为原型验证人工设定；
- 待确认：来源或数值尚未确认。

标签应出现在订单、经轴、产能、工时和排程结果上。

### 9.3 格式统一

- 日期：`YYYY-MM-DD`；
- 日期时间：`MM-DD HH:mm`；
- 数量：米，最多保留 1 位小数；
- 工时：分钟或小时，并明确单位；
- 百分比：保留 1 位小数；
- 空值：统一显示“—”；
- 未建档：统一显示“待建档”，不得显示为 0。

### 9.4 状态颜色

- 绿色：通过、可用、已完成；
- 蓝色：计划中、执行中；
- 黄色：模拟、待确认、部分排入；
- 红色：阻断、异常、未排入；
- 灰色：未启用、无数据、已作废。

## 10. 模块七：Excel 导入预检查

### 10.1 导入流程

```text
选择文件
  ↓
识别工作表与表头
  ↓
检查必填字段、格式和重复编号
  ↓
生成导入预览与异常清单
  ↓
用户确认
  ↓
生成新的数据快照
```

### 10.2 检查内容

- 必要工作表是否存在；
- 表头名称是否匹配；
- 日期和数字格式是否有效；
- 订单、产品、织机和经轴编号是否重复；
- 产品—经轴—织机映射是否完整；
- 经轴米数是否为正数；
- 开始时间是否早于结束时间；
- 外部公式或引用是否能够读取；
- 新数据是否覆盖已确认数据。

### 10.3 导入结果

导入后必须生成：

- 数据快照编号；
- 成功记录数；
- 警告记录数；
- 阻断记录数；
- 未导入记录及原因；
- 与上一数据快照的差异。

## 11. 模块八：结果导出

建议支持以下导出内容：

- 订单任务清单；
- 一周整经计划；
- 逐轴台账；
- 模拟可执行织造计划；
- 未排任务和原因；
- 数据质量异常清单；
- 参数清单；
- 排程版本差异；
- 测试和审计记录。

导出文件必须包含：

- 排程编号；
- 数据快照编号；
- 参数版本；
- 导出时间；
- 数据状态说明；
- “模拟计划”或“正式计划”水印标识。

## 12. 模块九：测试与运行日志

### 12.1 自动化测试

需要继续覆盖：

- 空数据；
- 缺字段；
- 重复编号；
- 日期格式错误；
- API 不可用；
- 请求超时；
- 返回数据缺失；
- 导入失败；
- 参数修改；
- 版本保存和差异计算；
- 页面筛选、跳转和错误恢复。

### 12.2 运行日志

每次运行记录：

- 请求编号；
- 用户操作；
- 调用接口；
- 开始和结束时间；
- 是否成功；
- 错误编号；
- 排程编号；
- 数据和参数版本。

日志中不得记录密码、访问令牌或其他敏感信息。

## 13. 实施优先级

### 第一阶段：稳定性与数据透明

1. 数据质量中心；
2. 模拟参数管理；
3. `Failed to fetch` 错误分类；
4. 前后端服务状态；
5. 页面计划层级和数据来源统一。

### 第二阶段：基础数据维护

1. 订单任务池字段对齐；
2. 产品工艺主档；
3. 织机资源台账；
4. 经轴台账；
5. Excel 导入预检查。

### 第三阶段：追溯与协作

1. 排程版本保存；
2. 版本差异展示；
3. 人工调整审计；
4. 结果导出；
5. 测试和运行日志完善。

## 14. 建议工期

| 阶段 | 主要工作 | 建议工期 |
|---|---|---:|
| 第一阶段 | 数据质量、参数、错误处理、状态统一 | 3—5 个工作日 |
| 第二阶段 | 基础数据维护、导入预检查 | 5—8 个工作日 |
| 第三阶段 | 版本、差异、审计、导出 | 4—6 个工作日 |
| 验收与修正 | 测试、问题修复和文档更新 | 2—3 个工作日 |

工期以现有原型代码为基础估算，不包含真实 ERP/MES 对接和正式权限体系建设。

## 15. 验收标准

### 数据质量

- 缺失、重复、格式错误和模拟字段能够集中展示；
- 每个异常可以追溯到具体数据对象和来源；
- 能够导出待客户补充数据清单；
- 阻断数据不会被系统静默当成正常数据。

### 参数管理

- 所有模拟参数可以查看来源和确认状态；
- 修改参数可以保存版本和修改历史；
- 排程结果能够追溯到实际使用的参数版本。

### 稳定性

- 后端不可连接时不再只显示 `Failed to fetch`；
- 页面能够说明错误类型并提供处理建议；
- 局部接口失败不会清空已有排程结果；
- 服务恢复后可以重新连接和刷新数据。

### 基础台账

- 订单、产品、织机和经轴编号及状态口径一致；
- 真实、推导、模拟和待确认数据能够清楚区分；
- 页面日期、数量和状态格式统一。

### 版本与审计

- 每次排程均有独立编号、数据快照和参数版本；
- 可以查看历史排程并比较差异；
- 人工调整均有人员、时间和原因记录；
- 未经确认的计划不能标记为正式发布。

### 工程质量

- 后端和前端自动化测试全部通过；
- 前端生产构建成功；
- 新增接口具有异常响应测试；
- 关键页面完成真实浏览器验证。

## 16. 推荐启动项

建议首先实施以下三个模块：

1. 数据质量中心；
2. 模拟参数管理；
3. 前后端连接状态与错误分类。

这些模块不依赖领导对排产过程的最终判断，实施风险较低，同时能够直接提高当前原型的透明度和稳定性。完成后，再依次建设基础数据维护、排程版本、差异对比和结果导出。

## 17. 当前实施进度（2026-09-03）

第一阶段首个可用版本已经完成：

- 已增加“数据质量”独立导航和汇总页面；
- 已集中展示阻断、警告、提示以及涉及模拟/推导的数据数量；
- 已按订单、经轴、织机、产能、物料和工装自动归类数据问题；
- 已显示数据问题的影响、来源和建议补充动作；
- 已支持导出“待客户补充数据清单”CSV；
- 已增加“模拟参数”独立页面；
- 下次求解的开始日期、周期、适配模式、求解时间、物料约束和经轴约束已与顶部工具栏联动；
- 400 米/天、提前 120 分钟、整经 240 分钟、穿综 480 分钟等假设已集中展示并标记为模拟或待确认；
- 已增加后端在线状态、最近成功连接时间和重新连接入口；
- 已将网络不可达、90 秒超时、参数错误、服务端错误和返回格式异常转换为中文分类提示；
- 接口失败时不再只显示 `Failed to fetch`；
- 已新增相关自动化测试并完成真实浏览器检查。

本次没有调整排产优先级、工艺工时或联合排程规则。参数版本持久化、异常筛选、排程历史和版本差异仍属于后续工作。

### 第二阶段首个增量：Excel 导入预检查与候选快照

- 已增加“数据导入”页面，支持 `.xlsx`、`.xlsm` 文件，单文件上限 25MB；
- 已检查必要工作表是否缺失或为空，并执行现有业务解析和数据质量校验；
- 已展示导入数据与当前数据在产品、织机、任务、经轴和物料数量上的差异；
- 存在阻断问题时禁止保存，不再静默接纳结构不完整的数据；
- 预检查通过后可以保存候选数据快照，并展示快照编号、哈希、数据量、时间和备注；
- 候选快照明确标记为“已保存，未启用”，当前排程仍使用既有客户工作簿；
- 使用客户文件完成真实预检查：22 张表、0 个阻断、9 个警告，解析出 19 个产品、108 台织机、19 个任务、19 类经轴和 41 项物料；
- 前端 60 项自动化测试、后端 159 项自动化测试全部通过，前端生产构建通过，并完成真实浏览器检查。

该增量只建立安全的数据进入通道，不改变领导尚未确认的排产过程、工艺工时、任务优先级或算法约束。后续经确认后，再增加“启用快照”和排程版本关联能力。
