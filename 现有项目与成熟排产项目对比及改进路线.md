# 现有织造排产项目与成熟排产项目对比及改进路线

> 编制日期：2026年9月4日  
> 对比对象：当前 `cp_sat_demo/weaving_demo` 项目，以及 frePPLe、PyJobShop、Google OR-Tools、Timefold Quickstarts  
> 报告目的：判断现有项目所处阶段，识别与成熟排产系统的关键差距，并形成可执行的后续改进顺序。

## Executive Summary

- **现有项目已经超过普通算法演示。** 项目具备织机与经轴独占、产品—织机适配、停机窗口、冻结任务、物料约束、任务拆分、换轴换款、穿综穿筘、逐轴台账和七天执行校验，已经形成有纺织行业特征的排产原型。
- **当前最大差距不在求解器，而在计划闭环。** 排程看板与工况模拟已经能显示最终可执行结果，但任务池、工艺流程和织机资源仍可能读取 CP-SAT 初排结果，导致同一排程存在两套数量和机台口径。
- **不建议更换 OR-Tools，也不建议直接移植完整开源 APS。** 现有织造规则具有专用价值，应保留 Python、FastAPI、React 和 OR-Tools 技术路线，借鉴成熟项目的数据模型、计划版本、约束解释、情景比较和执行反馈能力。
- **后续应先统一最终计划，再建设版本、发布和真实数据接入。** 如果在结果口径尚未统一时直接增加发布功能，只会把不一致的数据固化下来。

## 1. 对比范围与判断标准

本报告不单纯以 GitHub 星标或代码量判断项目成熟度，而是按生产排产系统需要具备的能力进行比较：

1. 生产约束和优化目标是否完整。
2. 各工序、物料与资源能否形成统一计划。
3. 排程结果是否唯一、可追溯、可复现。
4. 是否具有草稿、审核、发布、冻结和回退流程。
5. 是否能够接收生产实绩并进行滚动重排。
6. 数据导入、校验和主数据治理是否完善。
7. 未排、延误和调整原因能否解释。
8. 是否具备数据库、权限、审计、部署和监控等生产化能力。
9. 是否具有足够的功能测试、契约测试和性能基准。

## 2. GitHub成熟项目解析

### 2.1 frePPLe：完整APS平台参照

[frePPLe](https://github.com/frePPLe/frepple) 是面向制造企业的开源供应链计划和高级计划排程平台。它不仅处理短期生产排程，还覆盖需求预测、物料、库存、采购、产能、供应商、仓库和多地点计划。

其规划思路包括：

- 以订单优先级和交期决定需求处理顺序。
- 优先从交期向前倒排，同时检查物料与产能。
- 无法准时满足时再向后正排，寻找尽早交付方案。
- 支持约束计划、非约束计划、按瓶颈资源安排和拉动式生产。
- 支持交互式甘特图、容量瓶颈分析和异常驱动工作流。

参考资料：

- [frePPLe项目主页](https://github.com/frePPLe/frepple)
- [frePPLe生产计划与排程能力](https://github.com/frePPLe/frepple/blob/master/doc/features.rst)
- [frePPLe计划任务、导入导出及Web API](https://github.com/frePPLe/frepple/blob/master/doc/command-reference.rst)

**对本项目的借鉴价值：** frePPLe最值得借鉴的不是具体求解算法，而是完整APS的系统边界，包括正式主数据、计划情景、订单与供应追溯、计划状态、任务执行、导入导出和ERP接口。

**不适合直接替换的原因：** frePPLe是通用且庞大的APS平台，当前项目中的实体经轴、穿综穿筘、目标织机和余轴消耗等织造规则仍需要大量行业定制。

### 2.2 PyJobShop：生产调度建模参照

[PyJobShop](https://github.com/PyJobShop/PyJobShop) 是基于约束规划的Python生产调度库，默认使用OR-Tools CP-SAT，也支持CP Optimizer。

其标准能力包括：

- 单机、并行机、流水车间、混合流水车间和柔性作业车间。
- 释放时间、交期、截止期和任意工序先后关系。
- 多种加工模式、可选任务和可消耗资源。
- 顺序相关换型时间、停机、无等待、无空闲和阻塞约束。
- 最小化最大完工时间、总延期、最大延期、提前量和换型时间等目标。
- 示例、文档、基准算例和自动化测试结构。

**对本项目的借鉴价值：** 可用于重新检查当前领域模型是否将“任务、工序、资源、模式、日历、换型和先后关系”抽象清楚，也可以用其标准算例思路建设算法性能基准。

**不建议直接替换的原因：** 它是建模库而非完整APS，并不直接提供真实经轴台账、织造业务页面、数据治理和计划发布流程；复杂的逐轴状态变化仍需自行扩展。

### 2.3 Google OR-Tools：现有求解引擎参照

[Google OR-Tools](https://github.com/google/or-tools) 是当前项目已经使用的优化求解工具。其CP-SAT支持区间变量、可选区间、机器独占、工序先后、布尔选择和多类组合优化约束。

官方柔性作业车间示例展示了：

- 一个工序可在多台候选机器中选择。
- 同一工单中的工序按顺序执行。
- 同一机器上的任务不能重叠。
- 通过目标函数优化最大完工时间。

参考资料：

- [OR-Tools项目主页](https://github.com/google/or-tools)
- [柔性作业车间CP-SAT示例](https://github.com/google/or-tools/blob/stable/examples/python/flexible_job_shop_sat.py)
- [CP-SAT排程和日历建模说明](https://github.com/google/or-tools/blob/stable/ortools/sat/docs/scheduling.md)

**判断：** 当前项目选用OR-Tools是合理的。现阶段没有证据表明更换求解器能够解决当前核心问题；结果不一致、计划易失和真实数据未启用都属于系统架构问题，而不是CP-SAT能力不足。

### 2.4 Timefold：滚动重排和约束解释参照

[Timefold Quickstarts](https://github.com/TimefoldAI/timefold-quickstarts) 提供员工排班、维修排程、食品包装、任务分配和项目作业排程等案例。

其中食品包装案例与生产排程较接近，将约束划分为不同层级：

- 硬约束：不能超过最大结束时间、操作员清洁时间不能冲突。
- 中等约束：尽量满足理想结束时间、尽量安排更多任务。
- 软约束：尽量缩短总生产周期。

参考资料：[Timefold食品包装排程示例](https://github.com/TimefoldAI/timefold-quickstarts/blob/stable/use-cases/food-packaging/README.md)

**对本项目的借鉴价值：** 计划锁定、允许未分配、增量调整、Hard/Medium/Soft分层评分以及约束解释方式，适合用来改善滚动排程和“为什么这样排”的说明。

**不建议迁移的原因：** Timefold主要使用Java/Kotlin，迁移成本较高；当前项目已经具有较完整的Python领域代码和CP-SAT测试基础。

## 3. 现有项目能力基线

### 3.1 已实现的核心能力

现有项目已经实现：

- 一周7天滚动排程。
- 织机候选组合和产品—织机适配。
- 织机同一时间只能执行一个任务。
- 经轴同一时间不能被多台织机占用。
- 物料总量约束和安全库存口径。
- 停机、维修和冻结区间。
- 已开工、已上轴和人工锁定任务保护。
- 任务拆分、最小批量和最大拆分份数。
- 落布、上轴、换轴、换款和穿综穿筘准备时间。
- 8层字典序目标，依次关注未排量、交期、最大延期、拆分、机台分散、换型、计划变动和利用率。
- 未排原因编码、任务级诊断和结果校验。
- 一周整经计划、逐轴台账和逐段织造计划。
- 每段织造绑定具体经轴。
- 经轴数量、提前两小时到位和七天周期验证。
- 经轴不足时补排整经或减少织造数量。
- 时间为竖轴、工艺流程为横轴的甘特图展示。
- 车间状态快照和期末状态预览。

核心实现位置：

- `weaving_demo/solver.py`：CP-SAT主求解器。
- `weaving_demo/simulation.py`：逐轴执行模拟和事件校验。
- `weaving_demo/shopfloor.py`：车间状态快照。
- `weaving_demo/api/service.py`：业务编排和接口结果生成。
- `frontend/src/scheduleView.ts`：最终可执行结果的前端转换。

### 3.2 当前测试基础

截至最近一次全量验证：

- 后端测试：159项通过。
- 前端测试：68项通过。
- 合计：227项通过。

现有测试已覆盖织机独占、经轴绑定、经轴容量、停机、冻结、物料不足、任务拆分、补排整经、七天边界、跨日展示和主要页面组件。

### 3.3 当前实际结果口径

当前同一个排程中存在两种结果：

| 结果层级 | 已排数量 | 使用织机 | 用途 |
|---|---:|---:|---|
| CP-SAT算法初排 | 14,158米 | 6台 | 部分任务池、工艺任务和织机资源接口仍在使用 |
| 逐轴校验后最终计划 | 8,321.666米 | 4台 | 排程看板和工况模拟展示 |

这不是单纯的页面显示差异，而是计划数据源没有统一。成熟系统允许保留“初排方案”和“执行修正方案”，但所有正式业务页面必须明确读取同一份已选定计划版本。

## 4. 与成熟项目的差距

### 4.1 综合对比

| 维度 | 现有项目 | 成熟项目常见能力 | 差距判断 | 优先级 |
|---|---|---|---|---|
| 行业规则 | 已有较强织造专用规则 | 通用规则丰富，但织造规则通常需定制 | 本项目具有优势 | 保持 |
| 求解模型 | CP-SAT初排后再做逐轴执行修正 | 单一模型或明确的分层计划链，最终结果唯一 | 最终口径未统一 | P0 |
| 工艺范围 | 整经—准备—织造较完整，水洗等后工序偏展示 | BOM、路线、物料、工序和产能端到端联动 | 后工序尚未真正约束化 | P2 |
| 数据来源 | Excel和模拟数据为主 | 数据库主档、接口同步、生效版本 | 数据仍不稳定 | P1 |
| 导入流程 | 可预览和保存候选快照，但不能正式切换算法数据源 | 上传、映射、校验、审核、启用和回退 | 缺少启用闭环 | P1 |
| 计划持久化 | 排程结果主要保存在内存 | 数据库存储、历史版本、重启不丢失 | 不能可靠追溯 | P0 |
| 计划生命周期 | 求解后直接展示 | 草稿、校验、审核、发布、冻结、撤回、结束 | 尚未形成 | P1 |
| 情景方案 | 有A/B/C/D约束诊断 | 多方案保存、指标比较、选定并提升为生产方案 | 只能诊断，不能管理方案 | P2 |
| 滚动重排 | 支持现场快照和模拟，但打开页面可能重新计算 | 固定输入版本、增量重排、变动成本和冻结区 | 可复现性不足 | P0 |
| 执行反馈 | 有模拟期末快照 | MES实绩、部分完工、停机、余料、在制品回传 | 仍是模拟闭环 | P2 |
| 结果解释 | 有未排原因和校验 | 稳定原因码、约束贡献、异常处理工作流 | 部分原因仍由前端推断 | P0 |
| 页面一致性 | 看板已统一最终计划，其他模块仍有初排口径 | 所有模块读取同一计划版本 | 存在业务风险 | P0 |
| 人工调整 | 以参数求解和结果展示为主 | 拖动、锁定、局部重排、冲突即时校验 | 操作闭环不足 | P2 |
| 权限审计 | 暂无正式机制 | 用户、角色、审核权限和操作日志 | 生产化缺口 | P3 |
| 部署运维 | 本地启动，无标准容器和迁移脚本 | 数据库、Docker、CI/CD、监控、备份 | 生产化缺口 | P3 |
| 性能验证 | 当前真实样例规模较小 | 大中小算例、持续基准、求解SLA | 扩展能力未验证 | P2 |

### 4.2 核心差距一：最终计划没有成为唯一事实来源

当前流程大致为：

```text
订单及现场状态
    ↓
CP-SAT织造初排
    ↓
生成一周整经计划和逐轴台账
    ↓
工况模拟重新展开准备、补排和逐轴织造
    ↓
最终可执行计划
```

这个分层思路可以保留，但必须明确：

- `initial_plan`只是算法中间方案。
- `final_plan`才是所有业务模块的默认数据源。
- 任何页面不得自行重新计算正式指标。
- 重新模拟必须生成新版本，不能悄悄改变原排程。

当前任务池、工艺流程和织机资源仍可能读取原始 `assignments`；前端则在 `scheduleView.ts` 中重算最终数量和未排原因。这是目前最高风险问题。

### 4.3 核心差距二：计划和输入数据没有完整版本绑定

现有结果虽然记录了排程编号、配置版本和部分数据哈希，但仍缺少完整的不可变版本关系：

```text
排程版本
├─ 订单数据版本
├─ 产品与工艺版本
├─ 织机及工装版本
├─ 库存版本
├─ 车间状态快照版本
├─ 算法配置版本
└─ 最终输出版本
```

工况模拟在没有显式传入快照编号时会读取最新车间快照，因此同一个排程编号以后重新打开，理论上可能得到不同结果。成熟系统需要做到“同一版本重复读取必然得到同一计划”。

### 4.4 核心差距三：排程结果尚未持久化

`weaving_demo/api/store.py` 使用内存字典保存排程结果。后台重启后，排程历史和最近一次结果都会丢失。

这会直接影响：

- 领导审核时无法确保看到相同结果。
- 无法比较两个版本的调整内容。
- 无法追溯某个任务为什么被安排到某台织机。
- 无法可靠执行计划发布、撤回和回滚。

### 4.5 核心差距四：真实数据还没有形成启用机制

当前Excel导入模块能够生成预览和候选快照，但不会把选定快照切换为算法正式数据源。

成熟的数据导入应包含：

```text
上传文件
  → 自动识别表和字段
  → 字段映射
  → 主键及引用校验
  → 数据质量报告
  → 人工确认
  → 启用为正式版本
  → 可回退到上一版本
```

在客户数据尚不完整的情况下，可以继续使用模拟数据，但模拟数据也应该以正式版本方式管理，不能散落在代码默认值和多个JSON文件中。

### 4.6 核心差距五：生产执行仍停留在模拟层

现有车间快照和工况模拟为后续闭环打下了较好基础，但目前缺少真实执行事件：

- 实际开机和停机时间。
- 实际完成米数。
- 经轴实际剩余米数。
- 整经实际完成时间。
- 质量异常和返工。
- 缺料和临时插单。
- 人工换机或调整记录。

没有这些信息，系统只能回答“理论上如何排”，还不能持续回答“现场变化后如何重新排”。

## 5. 攀比成熟项目时不应误判的地方

### 5.1 不应因为frePPLe功能多就整体迁移

frePPLe成熟在完整APS框架，但其行业模型不是专门为织造和经轴设计。整体迁移意味着重做大量行业逻辑和前端交互，成本高且风险大。

### 5.2 不应因为PyJobShop抽象漂亮就替换当前求解器

PyJobShop同样默认使用OR-Tools。它能帮助规范建模，但无法自动解决当前计划版本、数据启用和执行反馈问题。

### 5.3 不应把甘特图完整等同于排产系统成熟

甘特图只是计划展示。成熟度更关键的判断是：

- 数据是否可信。
- 计划是否唯一。
- 变更是否可追溯。
- 结果是否经过审核。
- 执行结果是否能回写。
- 下一轮排程是否从真实现场状态开始。

## 6. 后续改进路线

## 阶段A：统一最终可执行计划（P0）

### 目标

消除14,158米与8,321.666米两套正式口径，让全系统只认一份最终计划。

### 实施内容

1. 在后端生成正式结果结构：

   - `final_assignments`
   - `final_kpi`
   - `final_unscheduled`
   - `final_process_events`
   - `final_beam_ledger`
   - `final_validation`

2. 保留 `initial_assignments` 供算法诊断，不再作为业务页面默认数据。
3. 任务池、排程看板、工艺流程、织机资源、整经计划和工况模拟统一读取 `final_*`。
4. 把最终未排原因放到后端生成，前端只负责展示。
5. 利用率按真正可用机时计算，扣除维修、停机和禁排时间。
6. 为所有业务接口增加同一排程版本的一致性测试。

### 验收标准

- 所有页面的已排总量一致。
- 所有页面的使用织机数量一致。
- 每段织造都能追溯到任务、产品、织机和经轴。
- 未排总量与需求量完成对账。
- 页面刷新不触发正式结果变化。

### 实施状态（2026-09-04）

阶段A已完成并通过回归测试。后端以 `final_schedule` 作为正式结果容器，并将其中的最终
`assignments`、`kpi`、`unscheduled`、工艺事件、逐轴台账和校验结果投影到现有业务接口；
原CP-SAT初排完整保留在 `initial_plan`，仅作为求解诊断和缩减对账证据。

- 排程看板、任务池、织机资源、周整经/织造计划、工艺甘特图和工况模拟已统一读取最终结果。
- 工况模拟页面默认读取求解时固化的结果与现场快照，不因刷新改用最新快照重算。
- 前端不再重算最终KPI或未排原因；兼容逻辑只服务于旧版、无 `final_schedule` 的历史数据。
- 新排程完成后会同时刷新工艺进度与甘特图，避免同一页面保留上一版本数量。
- 前后端服务端口统一为 `http://127.0.0.1:8000`，消除本地 `Failed to fetch` 配置冲突。
- 自动化验收结果：后端159项、前端70项，共229项测试通过；生产构建通过。

## 阶段B：排程持久化和计划版本（P0-P1）

### 目标

确保后台重启不丢计划，同一个排程版本可以重复查看、比较和追溯。

### 实施内容

1. 引入关系数据库，原型阶段可使用SQLite，正式部署建议PostgreSQL。
2. 建立排程版本表、排程任务表、排程事件表、经轴分配表和校验结果表。
3. 将每个排程版本绑定到订单、主数据、库存、车间快照和算法配置版本。
4. 建立父子版本关系，记录每次滚动重排从哪个版本产生。
5. 提供版本对比：新增、删除、换机、改期、数量变化和经轴变化。

### 验收标准

- 后端重启后历史排程仍可查看。
- 相同版本重复读取结果完全一致。
- 能查看两个版本之间的任务变化。
- 能定位每个结果所使用的输入数据和参数。

## 阶段C：审核、发布和冻结流程（P1）

### 目标

让排程从“算法结果”变成可由计划员正式确认和下发的生产计划。

### 建议状态

```text
草稿
  → 求解完成
  → 校验通过
  → 待审核
  → 已发布
  → 执行中
  → 已结束
```

校验失败或数据不完整时不得发布。已发布计划若需改变，必须生成新版本并保留差异记录。

### 实施内容

- 审核意见和驳回原因。
- 发布人、发布时间和发布范围。
- 冻结区内任务保护。
- 撤回、作废和重新发布。
- Excel/JSON计划导出。
- 发布前检查清单。

## 阶段D：真实数据导入和主数据治理（P1）

### 目标

逐步从模拟数据过渡到客户真实数据，同时保持系统可运行。

### 优先向客户补充的数据

1. 订单编号、产品、数量、真实交期、优先级和是否允许拆分。
2. 产品与织机的明确适配关系。
3. 产品在不同织机上的真实效率。
4. 织机班次、停机计划和维修日历。
5. 实体经轴编号、品番、总长度、剩余长度、所在位置和可用时间。
6. 整经机数量、效率、换型时间和班次。
7. 钢筘、综丝、边撑等工装库存和占用状态。
8. 原纱库存、预计到货和批次可用时间。
9. 穿综穿筘、上轴、落布等实际工时统计。
10. 水洗、涂层、验布和入库工序的设备与产能。

### 实施方式

- 真实数据缺失时继续使用模拟值，但必须标记来源和可信等级。
- 每个临时参数设置负责人、默认值和待确认状态。
- 数据质量问题应阻止发布或转化为明确风险，不能静默忽略。

## 阶段E：滚动重排和生产反馈（P2）

### 目标

从“一周静态模拟”升级为“每天可以根据现场变化重新计算”。

### 实施内容

1. 接收实际开工、完工、停机、余轴和缺料事件。
2. 锁定已执行部分，未执行部分进入重新求解。
3. 将计划变动数量、换机数量和延迟变化作为优化目标。
4. 支持紧急订单插入和故障模拟。
5. 在重新发布前展示新旧方案差异。

### 验收标准

- 已经执行的任务不会被移动。
- 冻结区任务原则上保持不变。
- 系统能解释每一次调整的触发事件。
- 新方案能量化交期、换型、利用率和计划稳定性的变化。

## 阶段F：扩展完整工艺链（P2）

### 目标

把后续工艺从甘特图推导展示升级为实际资源约束。

建议逐步纳入：

- 水洗。
- 涂层。
- 验布。
- 包装。
- 入库。

每新增一道工序都应具备任务、资源、日历、产能、在制品转移时间和工序先后约束，避免只增加一条视觉甘特条而没有进入算法。

## 阶段G：性能和工程生产化（P2-P3）

### 性能验证

建立固定基准数据集：

- 小规模：20个任务、100台以内设备，用于功能回归。
- 中规模：200至500个任务，用于日常求解性能测试。
- 大规模：1,000个以上任务，用于评估分解求解、并行和时间上限策略。

每次算法修改记录：

- 求解时间。
- 最优性状态和目标值。
- 已排率、准时率和最大延迟。
- 换型、换轴和计划变动次数。
- 最终执行校验是否通过。

### 工程建设

- `pyproject.toml`或锁定的依赖清单。
- 数据库迁移工具。
- Docker和标准启动方式。
- GitHub Actions或其他CI流程。
- API鉴权和用户权限。
- 操作审计日志。
- 错误监控、健康检查和结构化日志。
- 数据库备份和恢复演练。

## 7. 推荐实施顺序

```text
阶段A：统一最终计划
    ↓
阶段B：持久化与版本绑定
    ↓
阶段C：审核、发布与冻结
    ↓
阶段D：真实数据启用
    ↓
阶段E：现场反馈与滚动重排
    ↓
阶段F：完整工艺链
    ↓
阶段G：规模化与生产部署
```

不能颠倒的关键关系是：

- 没有唯一最终计划，就不能可靠发布。
- 没有持久化和输入版本，就不能可靠追溯。
- 没有发布版本，就无法判断现场执行偏差。
- 没有真实反馈，就不能形成真正的滚动优化。

## 8. 第一阶段建议任务清单

下一轮开发建议只聚焦以下任务，暂不扩大到其他工序：

1. 定义统一的 `FinalSchedule` 后端数据结构。
2. 将逐轴模拟生成的结果写入排程结果，而不是由页面打开时重新生成。
3. 固定求解时使用的车间快照编号和模拟参数。
4. 修改任务池、织机资源和工艺流程接口，统一读取最终结果。
5. 删除前端对最终业务指标和未排原因的二次推断。
6. 增加跨模块一致性测试。
7. 增加“保存新车间快照后，旧排程不得变化”的回归测试。
8. 在页面明确区分“算法初排”和“最终可执行计划”，默认只展示最终计划。

## 9. 后续需要确认的问题

这些问题会影响真实排产模型，但不阻塞当前第一阶段：

1. 整经机实际有多少台，是否能同时整经，以及各机的效率和适配差异。
2. 同一根经轴能否分多次织造，剩余长度如何处理。
3. 经轴从整经完成到穿综穿筘、上轴之间是否存在运输或等待时间。
4. 订单是否允许部分交付，允许拆分到几台织机。
5. 换款、换轴和穿综穿筘的真实时间是否随产品组合变化。
6. 七天计划是自然日连续生产，还是存在班次和休息日。
7. 水洗及其他后工序是否会反向限制织造节奏。
8. 领导需要审核的是整周计划、每天计划，还是任务级变更。

## 10. 结论

现有项目最准确的定位是：

> 具有织造行业约束、逐轴校验和业务解释能力的可验证排产原型，正在从算法演示向可落地APS系统过渡。

与成熟项目相比，现有项目的行业规则并不弱，甚至在经轴追踪方面更贴近客户现场。真正需要补齐的是：唯一计划结果、数据版本、结果持久化、审核发布、真实数据启用和执行反馈。

因此，下一步不应重写算法或更换求解器，而应先把逐轴校验后的最终可执行计划提升为后端唯一业务结果。完成这一步后，计划版本、发布流程和客户真实数据接入才具有可靠基础。

## 参考项目

- [frePPLe/frepple](https://github.com/frePPLe/frepple)
- [frePPLe功能说明](https://github.com/frePPLe/frepple/blob/master/doc/features.rst)
- [frePPLe命令和计划工作流](https://github.com/frePPLe/frepple/blob/master/doc/command-reference.rst)
- [PyJobShop/PyJobShop](https://github.com/PyJobShop/PyJobShop)
- [google/or-tools](https://github.com/google/or-tools)
- [OR-Tools柔性作业车间示例](https://github.com/google/or-tools/blob/stable/examples/python/flexible_job_shop_sat.py)
- [OR-Tools排程建模说明](https://github.com/google/or-tools/blob/stable/ortools/sat/docs/scheduling.md)
- [TimefoldAI/timefold-quickstarts](https://github.com/TimefoldAI/timefold-quickstarts)
- [Timefold食品包装排程示例](https://github.com/TimefoldAI/timefold-quickstarts/blob/stable/use-cases/food-packaging/README.md)

## 报告说明与限制

- 本报告基于2026年9月4日可访问的GitHub公开仓库资料和当前本地项目代码。
- 开源项目的社区版与商业版能力可能不同，本报告仅将公开资料中可确认的能力作为参照。
- 当前项目仍使用模拟和推导数据，算法结果只能用于原型验证，不能直接代表客户现场最终可执行产能。
- GitHub项目规模和受欢迎程度仅作为背景，不作为技术适配性的决定依据。
