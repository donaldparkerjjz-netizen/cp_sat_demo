# 工厂排工排产系统产品需求文档（PRD）

版本：v1.0    状态：初稿
适用范围：离散与流程制造工厂的车间级排工排产，预留与 ERP、MES、考勤系统的集成。
撰写依据：已完成的 CP-SAT 排产引擎、多阶段流水线、交互式甘特图、排产分析、Excel 生产表单抽取等验证成果，以及面向智能排产的客户需求。

---

## 1. 背景与问题

当前多数工厂排产仍高度依赖人工经验与 Excel，普遍面临以下问题。

- 排产效率低：排产会议后需专职文员逐条录入系统，日均二至四小时，且易出错。
- 插单响应滞后：临时加单或急单频繁，人工重排耗时，难以快速评估插单对现有计划的影响。
- 约束未量化：产品与机台适配、换模与清机时间、班次产能、返工专线、员工工价均衡等关键约束未纳入系统，计划可执行性受限。
- 多车间与多资源割裂：不同车间排产逻辑差异大，例如连续批量、多机台单班、多产线阶梯计价等，口径不统一。
- 数据孤岛：排产数据分散于 ERP、Excel、考勤、设备系统，缺乏统一整合与智能分析。
- 缺乏可视化与指标：排产结果不可见、不可分析，难以事前模拟与事后复盘。

## 2. 产品目标

实现从人工排产到智能排产，并做到可配置、可复用。

1. 排产自动化：覆盖多车间、多类资源的自动编排，排产耗时从数小时缩短到分钟级。
2. 硬约束可执行：任何方案都满足产品与资源适配、单班唯一、产能上限、换模与清机、返工专线等硬约束。
3. 软约束可择优：在可行方案中按优先级、交期、工价均衡、换模最小化、利用率等目标择优。
4. 可视化交互：机台按时间的甘特图，支持拖拽、颜色区分、换模标注、交期线。
5. 智能分析：利用率、瓶颈、产能与需求匹配、拖期与准交、工价均衡仪表盘。
6. 数据贯通：与 ERP 和 MES 双向集成，消除手工录入，为工资核算与绩效考核提供数据。
7. 跨行业通用：资源、工序、班次、约束、目标均可配置，适应不同车间与行业。

## 3. 用户与角色

| 角色 | 主要诉求 |
|------|----------|
| 排产计划员 | 快速生成可执行方案、处理插单、在甘特图微调 |
| 车间主管与班组长 | 查看本车间排产、协调班组与人员、处理异常 |
| 生产文员 | 排产结果下发、与 ERP 单据核对、报表 |
| 财务与核算 | 计件工价、工资核算、工价均衡 |
| 生产经理与高层 | 看板、KPI、产能与需求、投产决策 |
| 系统管理员 | 基础数据、资源与工艺与工价与换模矩阵维护、权限 |

## 4. 术语定义

| 术语 | 说明 |
|------|------|
| 排产单元 | 排产的基本单位：资源、产品型号、班次、班组的组合 |
| 资源 | 机台、产线、班组、人员、返工专线等可占用对象 |
| 工序与工艺路线 | 一个工单需依次经过的加工步骤 |
| 工单与订单 | 待排产的需求，包含产品、数量、交期、优先级、来源单据、类型 |
| 班次 | 八小时（早中晚三班）或十二小时（白夜两班），可配置 |
| 节拍与产能 | 单位时间（班或小时）的标准产出，对应产能上限约束 |
| 换模与清机 | 同一资源更换产品型号所需准备时间，需预留 |
| 硬约束 | 不可违反的可行性条件 |
| 软约束 | 用于可行方案间择优的优化目标 |
| 排产类型 | 正常、委外、返工，对应不同来源单据与计件规则 |
| 插单与重排 | 新增订单或需求变更后的重新排产与任务调剂 |
| 甘特图 | 资源按时间的排产可视化视图 |

## 5. 总体架构

数据源（ERP、Excel、预测、考勤、设备）
进入数据接入层（WebAPI、导入、同步）
进入基础数据建模（资源、产品、工艺、班次、日历、工价、换模矩阵）
进入 CP-SAT 排产引擎（硬约束与软目标，多目标择优）
得到排程结果（资源、产品、班次、班组、数量、时段）
进入可视化与排产分析（甘特图、利用率、瓶颈、产能与需求、工价均衡）
回写 ERP 与 MES（工单、流程卡、产线日排产），进入工资核算、看板、审计。

## 6. 核心排产模型

### 6.1 排产要素
- 资源集合：机台、产线、班组、人员、返工专线，均可配置。
- 产品与工艺：产品型号、工艺路线（先序工序）、标准产能与节拍、资源与产品适配。
- 需求（工单）：来源（合同、预测、委外、返工）、产品、数量、交期、优先级、类型。
- 时间轴：班次长度（八或十二小时）、工厂日历（节假日、休息）、换模与清机。
- 排产单元：资源、产品、班次、班组的组合；一个资源按产品与班次可能对应多个排产任务。

### 6.2 硬约束（可行性）
| 约束 | 说明 | CP-SAT 建模 |
|------|------|-------------|
| 产品与资源适配 | 部分产品只能上特定机台或产线 | AddAllowedAssignments 或可选区间 |
| 资源单班唯一 | 一个资源一个班次只做一个产品 | 每班每资源占用不超过一 |
| 产能上限 | 排产不超过该资源该产品该班次标准产能 | 工序时长等于数量除以产能 |
| 返工专线 | 返工任务只排指定返工机台或产线 | 固定资源集 |
| 先序与工艺 | 同工单工序按顺序 | 先后约束（前工序结束不晚于后工序开始） |
| 换模与清机 | 同资源换产品需预留时间 | AddTransition 或相邻空档 |
| 可选：物料齐套 | 需物料就绪才可开工 | 引入物料时间 |
| 可选：人力与班组 | 班组与人员人数及技能约束 | 可选区间加资源独占 |

### 6.3 软约束（优化目标）
| 目标 | 说明 | 度量 |
|------|------|------|
| 优先级优先 | 按业务优先级排产，急单最高 | 加权完成或加权拖期 |
| 交期与准时 | 尽量不拖期 | 总拖期最小化 |
| 工价均衡 | 平衡各班组与员工累计计件收入 | 偏差不超过正负百分之二十（或最大值减最小值） |
| 换模最小化 | 同资源尽量连续同型号或同系列 | 换模次数或时间累计 |
| 产能利用率 | 减少资源空闲 | 加权空闲时间 |
| 库存与在制均衡 | 避免过度压单 | 在制或库存偏差 |

### 6.4 目标函数与求解
- 求解器：OR-Tools CP-SAT。
- 原语：区间变量（start、size、end）、AddNoOverlap（资源独占）、先后约束（工艺顺序）、AddAllowedAssignments（资源与产品适配）、AddTransition（序列相关换模）、可选区间与布尔（资源指派、返工专线）。
- 多目标：加权求和，或字典序（优先级、交期、工价均衡、换模最小化、利用率）。
- 求解策略：设求解时间上限，返回最优解（OPTIMAL）或当前最优可行解（FEASIBLE 且给出 gap 值）。

### 6.5 时间粒度与规模
- 时间单位可配置：班、日、小时、分钟（使用整数，避免浮点）。
- 典型规模（数百机台、数百工单、百班次）应在秒级到分钟级给出方案；更大规模可分层或分车间求解。

## 7. 功能需求

### 7.1 基础数据管理（P0）
- 资源台账：机台、产线、班组、人员、状态、节拍、是否返工线。
- 产品与工艺：产品型号、工艺路线、标准产能与节拍、BOM（可选）。
- 资源与产品适配矩阵。
- 换模与清机矩阵（产品对与时间）。
- 工价表：按产品、工序、班次、阶梯计价。
- 班次与工厂日历。
- 用户与权限（RBAC）。

### 7.2 数据接入（P0、P1）
- 合同与订单（ERP WebAPI 实时查询）。
- 客户滚动要货预测（Excel 导入，每周）。
- 委外采购单、返工调拨单（ERP）。
- 库存与在制（可选）。
- 考勤（钉钉，用于工资核算）。

### 7.3 智能排产引擎（P0 核心）
- 正向排产：按优先级与交期顺序，在满足硬约束下生成初始方案。
- 三种排产类型：正常（合同加预测）、委外（委外采购单）、返工（返工调拨单）；对应不同 ERP 单据与计件规则。
- 分班排产：自动根据周目标产量与标准产能计算所需班次数，生成相应数量的排产任务（流程卡）。
- 插单与重排：业务员仅指定某订单必须在某天完成；系统在硬约束下自动寻优机台与班次；被挤任务按 P1（同产品其他可用机台）、P2（原机台顺延）、P3（跨机台加跨日期，需人工确认）推荐调剂；同日同资源换产品自动计算换模并插入空闲时段，若超班次可用时间则提示不可行并给替代方案。
- 多目标择优：优先级、交期、工价均衡、换模最小化、利用率。

### 7.4 甘特图可视化（P0）
- 机台按时间的甘特图，按产品与订单着色。
- 视图切换：按资源、按工序、按班组。
- 交互：拖拽调整、缩放、平移、悬停详情、图例、交期线、超载提示。
- 换模与清机标注。
- 导出：图片、Excel。

### 7.5 排产分析与决策（P0）
- 机台利用率、瓶颈工序与资源。
- 产能与需求（满足度、超载预警）。
- 排产前后对比、插单影响评估。
- 工价均衡仪表盘：各班组与员工当月累计计件收入与平均线的偏差（不超过正负百分之二十）。
- 拖期率、准交率、在制统计。

### 7.6 集成与回写（P1）
- 排产结果写回 ERP（产线日排产、流程卡、工单），消除手工录入。
- 触发下游单据（领料、报工、入库）。
- 与 MES 报工、仓库入库二次核对。

### 7.7 工资核算（P1，可选）
- 计件工资按排产任务、入库数量核算（报工数与审核入库数二次比对）。
- 复配与阻尼车间：对接钉钉考勤，日工单班组计件总价均摊至实际出勤人员；处理顶班、请假、调班，及有底薪与无底薪差异。
- 阶梯计价（如大、中、小机按当日车数分档计件）。
- 仓库员工按出库重量核算。

### 7.8 AI 智能助手（P2，可选）
- 自然语言与语音指令：排产、插单、查询、调剂。
- 降低使用门槛；需明确权限与安全边界（仅执行已授权操作）。

## 8. 关键业务流程

主流程：需求收集（订单、预测、委外、返工）进入基础数据校验，然后自动排产，进入甘特图确认与微调，然后下发 ERP，进入报工与入库，然后工资核算，最后分析与看板。

插单与重排流程：接收插单需求，系统寻优（机台与班次），被挤任务调剂推荐（P1、P2、P3），如涉及 P3 则人工确认，甘特图更新，然后回写。

工价均衡流程：事前优化（模拟排产阶段纳入均衡目标），事后调整（甘特图拖拽换型号，实时计算工资变化），再次调整直到偏差不超过正负百分之二十。

## 9. 非功能需求

- 性能：常规规模排产不超过一分钟；插单与重排不超过十秒；甘特图交互流畅（缩放与平移无卡顿）。
- 可配置性：资源、工艺、班次、日历、约束、目标均可配置，跨车间与跨行业可复用。
- 数据一致性：与 ERP 双向同步、有审计与幂等，避免重复写单。
- 安全：RBAC 权限、敏感数据（工资）脱敏、操作留痕。
- 可扩展：约束与目标插件化；多车间并行求解；支持后续接入 AI 助手。
- 可用性：Web 界面，多端访问；异常输入给出明确提示与替代方案。

## 10. 验收指标（KPI）

| 指标 | 目标 |
|------|------|
| 排产耗时 | 数小时降至十分钟以内 |
| 人工录入消除率 | 排产数据自动写回 ERP，达到百分之百 |
| 插单响应 | 十秒内给出可执行方案 |
| 方案可执行率 | 百分之九十八以上（满足硬约束） |
| 工价偏差 | 班组与员工月累计计件收入偏差不超过正负百分之二十 |
| 换模次数 | 不劣于人工，且接近最优 |
| 机台利用率 | 环比提升（目标百分之五以上） |
| 准交率与拖期率 | 显著改善 |

## 11. 迭代规划（对齐六模块分步）

| 阶段 | 范围 | 对应模块 |
|------|------|----------|
| Phase 1（第一步） | 排产引擎、甘特图、基础数据、分析 | 智能排产引擎、甘特图可视化、系统管理、分析 |
| Phase 2（第二步） | 数据接入、金蝶双向集成、排产回写 | 金蝶集成模块、数据接入 |
| Phase 3（第三步） | 插单重排增强、AI 智能助手 | AI 智能助手、高级排产 |
| Phase 4（第四步） | 工资核算、钉钉考勤、仓库核算 | 工资核算模块 |

## 12. 与现有已验证成果的映射

我们已用 CP-SAT 与实际数据验证了本 PRD 的核心能力，可直接复用到产品中。

| PRD 能力 | 已验证成果 |
|----------|-----------|
| 约束排产引擎（硬与软约束、多目标） | aps_engine.py、scheduler.py（CP-SAT，OPTIMAL） |
| 多阶段流水线排产（先序与资源独占） | schedule_finishing.py（整经、织造、水洗、涂层、验布） |
| 交互甘特图（资源按班次、换模标注、悬停、缩放） | aps_gantt.html、finishing_gantt.html |
| 排产分析（利用率、瓶颈、产能与需求、拉动） | analysis.py 与分析图 |
| 生产表单解析与抽取（参考 Excel 表单结构） | extract_data.py（益丰生产管理表单） |
| 结果写回 Excel（资源按班次排产矩阵） | 减振车间排产数据.xlsx |

## 13. 风险与假设

- 基础数据质量：机台产能、适配矩阵、换模矩阵、工价需权威维护，否则影响可行性。
- 求解规模与时间：大规模需分层或分车间或放宽时间上限，保证在可接受时间内给出可行解。
- 口径统一：多车间的排产单元、单位（片每班、米每班、小时）、计件规则需统一建模。
- 集成稳定性：与 ERP、MES、考勤接口的稳定性、重试与幂等。
- 目标权重：多目标权重默认值需与业务确认，避免过度偏置。

## 14. 开放问题与待确认

1. 排产粒度：日、班、小时（默认班）。
2. 人力与班组是否为硬约束（多班多人、技能矩阵）。
3. 物料齐套是否纳入排产（与采购、库存联动）。
4. 优化目标的主次与默认权重。
5. 单位与产能口径的权威定义。
6. AI 助手的操作边界与安全合规。

---

本文档用于统一工厂排工排产的范围、模型与验收口径，后续按 Phase 1 至 Phase 4 迭代落地。
